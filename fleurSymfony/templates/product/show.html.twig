{% extends 'base.html.twig' %}

{% block title %}{{product.name}} - FlowerStore{% endblock %}

{% block content %}
    {#<h1> {{product.name}} </h1> {{dump(product)}}#}

    <div class="row">
        <div class="col-md-5"> 
            <img  src="/uploads/images/{{product.illustration}}" alt="Image de {{ product.name }}" class="img-fluid"/>
        </div>
        <div class="col-md-7 my-auto">
            <h5> {{ product.name }} </h5>
            <p>{{product.subtitle}}</p> 
            <span class="product-page-price">{{(product.price / 100)|number_format(2,',','.') ~ ' €'}}</span>
            <hr>
            <p>
                {{product.description}}
            </p>
            <a href="" class="btn btn-primary"> Ajouter au panier</a>
        </div>
    </div>

{% endblock %}
